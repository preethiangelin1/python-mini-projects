# GitHub User Activity CLI

A simple command-line tool to fetch and display recent public activity of a GitHub user.

## 🚀 Features

- Fetches public events of a GitHub user
- Handles invalid usernames
- Handles network errors
- Colored CLI output (header, warnings, errors)
- Simple and clean output

---

## 📦 Requirements

- Python 3.8+
- requests
- colorama

Install dependencies:

```bash
pip install -r requirements.txt